
//in class activity

You are going to create a constructor function called Product.

Inside the constructor,use this to assign values to the object.

step 2:

Create 3 different products using the new keyword

example,

const product1 = new Product("laptop",7500,"3);


create two more products with different details

step 3:

Print all the products to the console.

step4:

simple calculation practice. After creating the objects,print: The total value of each product(price x quantity)


example:

console.log(product1.price * product1.quantity)



(3/3/2026 assignments)

Task 1:
Create a function that:

Takes two arrays

Returns a new merged array

Uses spread

Does NOT mutate either original array

Example:

const a = [1, 2];
const b = [3, 4];

combineArrays(a, b);
// [1, 2, 3, 4]



Task 2:

Write a function called createGreeting that takes two parameters:

name (default value: "Guest")

greeting (default value: "Hello")

Requirements:

If no arguments are passed, return:
"Hello, Guest!"

If only name is passed, use the default greeting.

If both arguments are passed, use both custom values.



Task 3:

Write a function called calculateTotal that takes:

price

tax (default value should be 16% of the price)

Requirements:

Return the total price including tax.

If tax is not provided, calculate it automatically as 16% of the price.

If tax is provided manually, use the provided value instead of calculating it.